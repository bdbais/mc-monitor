<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_dim"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- I due segni. Stanno nella riga e non dentro il pannello delle azioni
         perche' segnare qualcuno e' un gesto da fare scorrendo l'elenco: se
         costa due tocchi in piu' nessuno lo fa, e il cruscotto resta vuoto. -->
    <ImageButton
        android:id="@+id/preferito"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Preferito"
        android:padding="8dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_star"
        android:tooltipText="Preferito" />

    <ImageButton
        android:id="@+id/sorvegliato"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Tieni d'occhio nel cruscotto"
        android:padding="8dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_occhio_mc"
        android:tooltipText="Cruscotto" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/action"
        style="@style/McButton.Text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="0dp"
        android:text="Azioni" />
</LinearLayout>
